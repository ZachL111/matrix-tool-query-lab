[project]
name = "matrix-tool-query-lab"
version = "0.1.0"
description = "Package a Python local lab for query analysis with seeded input scenarios, deterministic summary checks, and documented operating limits."
requires-python = ">=3.11"
