# Matrix Tool Query Lab Walkthrough

This note is the quickest way to read the extra review model in `matrix-tool-query-lab`.

| Case | Focus | Score | Lane |
| --- | --- | ---: | --- |
| baseline | file span | 199 | ship |
| stress | terminal width | 204 | ship |
| edge | argument risk | 247 | ship |
| recovery | report density | 198 | ship |
| stale | file span | 163 | ship |

Start with `edge` and `stale`. They create the widest contrast in this repository's fixture set, which makes them better review anchors than the middle cases.

`edge` is the optimistic case; use it to make sure the scoring path still rewards strong signal.
